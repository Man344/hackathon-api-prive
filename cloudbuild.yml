steps:
  - name: 'gcr.io/cloud-builders/docker'  
    entrypoint: docker
    args: [ 'build', '-t', '"${_DOCKER_REPO}/${_IMAGE}:${_TAG}"', '-f', 'Dockerfile', '.' ]
images:
  - 'europe-west1-docker.pkg.dev/cagip-hackathon-eq11-inn0-28/${_REPOSITORY}/${_IMAGE}'
